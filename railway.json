{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd orchestrator && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd orchestrator && npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}
